<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>
    <% if (username) { %>
      <form class="form-inline" action="/logout" method="POST">
        <label for="username">Logged in as: <%= username %></label>
        <button type="logout" class="btn btn-primary">Logout</button>
      </form>
      <% } else { %>
    <form class="form-inline" action="/login" method="POST">
      <label for="longURL">Login:</label>
      <input class="form-control mr-sm-2" type="text" name="username" style="width: 200px; margin: 1em">
      <button type="login" class="btn btn-primary">Login</button>
    </form>
    <% } %>
  </nav>
</header>